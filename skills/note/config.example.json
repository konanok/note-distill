{
  "_comment": "note-distill 配置模板。由 /note-config 自动生成，也可手动编辑。",

  "adapter": "local-markdown",

  "_comment_path": "根据 adapter 填写对应的路径字段",
  "output_dir": "",
  "_obsidian_example": "如用 obsidian，替换上面 output_dir 为 obsidian_vault_path",

  "templates_dir": "~/.config/note-distill/templates",
  "_templates_dir_comment": "用户自定义模板目录，可省略（默认值如上）",

  "default_template": "til",
  "_default_template_comment": "不指定模板时的默认模板名，出厂默认 til。可选: til, design, technical，或用户自定义的任何模板名",

  "candidate_selection": {
    "default_behavior": "auto",
    "auto_pick_strategy": "oldest",
    "max_pick_options": 5
  },

  "candidate_analyzer": {
    "provider": "claude",
    "model": "claude-haiku-4-5-20251001",
    "fallback": "heuristic"
  }
}
