---
title: "{{title}}"
type: investigation
created: { { datetime } }
updated: { { datetime } }
ai-generated: true
reviewed: false
source: note-distill:{{platform}}:{{session_id}}
tags: [investigation, { { domain_tags } }, ai-generated, TODO]
follow-up: []
---

# {{title}}

{{tldr}}

<!--
一段话全貌：现象 + 根因 + 修复方向。100-200 字。
不是独立 section，而是标题后的第一段，让人 30 秒知道全貌。
-->

## 问题现象

{{symptom}}

<!--
描述异常表现，具体到"什么场景下看到什么"：
- 现象：报错信息、数据偏差、行为异常
- 触发条件：什么操作 / 什么数据会触发
- 影响范围：哪些用户 / 地域 / 机型

不说"有问题"，说"库存值停留在 127 而非 0"。
-->

<!--
可选：## 复现方法
复现不直觉时加——需要特定数据 / 时序 / 环境才能触发，别人看现象描述不一定能复现。
现象描述本身足够时不加。插入位置：问题现象之后、根因分析之前。
-->

## 根因分析

{{root_cause}}

<!--
先放根因链（5 Whys 风格），再写一段总结。

根因链格式：
```
为什么 X？
└→ 因为 Y
   └→ 因为 Z
      └→ 根因：具体组件 / 代码行 / 配置项
```

每层一句话，最后一层指向具体技术锚点（文件:行号、配置项、服务名）。

如适用，在链后加正常 vs 异常路径对比图（ASCII 或 mermaid），用 ❌ 标注断裂点。
图可选：多组件流转且断裂点不直觉时加，简单问题不加。
画出正常路径 + 断裂点，不要只画断的那条。

总结段：一段话概括根因，呼应链的最后一层。
-->

## 修复方案

{{fix}}

<!--
多种方案对比，每个方案写：思路 + 优点 + 缺点，最后说明选择。

### 方案 A：xxx
- 思路：...
- 优点：...
- 缺点：...

### 方案 B：xxx
- 思路：...
- 优点：...
- 缺点：...

**选择方案 X**，理由：...

选定方案的具体改动点用表格式：

| 改动 | 位置 | 说明 |
|------|------|------|
| 方法名 | 文件:行号 | 做了什么 |

如只有一种方案，不用对比格式，直接写改动点即可。
-->

<!--
可选：## 验证方案
验证步骤不直观、有多种验证维度时加。
验证很自然一句话说清时不加。插入位置：修复方案之后、遗留风险与待办之前。

可选：## 遗留风险与待办
有已知风险、未覆盖的边缘场景、有待和团队对齐的决策点时加。
方案干净无已知风险时不加。插入位置：验证方案之后（或修复方案之后，如无验证方案段）。
-->
