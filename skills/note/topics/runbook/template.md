---
title: "{{title}}"
type: runbook
created: { { datetime } }
updated: { { datetime } }
ai-generated: true
reviewed: false
source: note-distill:{{platform}}:{{session_id}}
tags: [runbook, { { domain_tags } }, ai-generated, TODO]
follow-up: []
---

# {{title}}

{{tldr}}

<!--
一段话全貌：什么操作 → 什么结果 → 为什么结果是"对但不对"（缺了什么前提/漏了什么步骤）→ 排查方向。100-200 字。
不是独立 section，而是标题后的第一段，让人 30 秒知道"这是不是我遇到的问题"。
-->

## 适用症状

{{symptoms}}

<!--
一句话概括问题模式，再列举具体症状。注意 runbook 的"症状"特征——
系统行为符合程序预期，但操作结果与意图不符：
- 做了什么操作 / 传了什么参数 / 数据是什么
- 系统返回了什么（符合预期的结果，但不是我想要的）
- 为什么这个结果是"对但不对"（缺了什么前提/漏了什么步骤）
- 触发条件（什么前置状态缺失会导致这个问题）

让读者 5 秒内判断"我也做了同样的操作、得到了同样的结果"。
与 investigation 的区别：investigation 描述"系统输出异常"，runbook 描述"系统输出正确但不是我想要的"。
-->

<!--
可选：## 前置知识
排查前需了解的关键概念、模块关系、术语缩写。
对话未讨论 → 写"（未在对话中讨论，待补充）"。
-->

## 问题链路

{{causal_chain}}

<!--
解释为什么漏了这一步/参数/数据会导致这个结果。注意与 investigation 的区别——
这里不是根因分析（没有代码缺陷），而是链路说明：正常操作流程是怎样，缺口在哪一环。

格式：先画正常流程 vs 异常流程对照（ASCII 或文字），再一句话总结。

例：
正常：配置 region → 纳管 CHC → 调度器按 region 分配 → 调度成功
异常：未配置 region → 纳管 CHC → 调度器查询 region 为空 → 跳过该 host → 调度失败

系统行为每一环都正确——调度器"跳过 region 为空的 host"是设计行为，不是 bug。

如对话未讨论链路，写"（问题链路未在对话中讨论，待补充）"。
-->

## 排查步骤

<!--
这是 runbook 的核心——必须是一份**可被逐行执行的诊断脚本**，让读者（人或 AI）不需要额外查文档就能照做。

每步一个 ### 标题，按逻辑递进排列（从外到内/从浅到深/从通用到特定）。

每步包含：
- 目标：这一步要确认/排除什么（操作/数据/参数是否遗漏或错误）
- 操作：具体命令、查询语句、日志搜索关键字、数据库查询语句、API 调用等。
  必须给出完整可执行的语句，不说"查日志"而说"`grep 'QUOTA_RESET' /var/log/cvm/*.log`"，
  不说"查数据库"而说"`SELECT * FROM t_host WHERE region IS NULL`"。
- 判断分支（必须有分支，不要平铺）：
  - 结果 A → 说明已确认到这一步，进入步骤 X
  - 结果 B → 说明问题就在这一步（操作/数据/参数缺失或错误）→ 定位成功
  - 结果 C → 当前线索不通，换方向排查，建议...
- 图示（按需）：多分支难以文字描述时，加 ASCII 决策树或 mermaid flowchart，
  让人一眼看清所有分支走向。

步骤数没有上下限，至少 2 步。

例：
### 1. 确认 host 的 region 字段

- **目标**：排除 region 为空导致调度跳过的可能
- **操作**：`SELECT host_id, region FROM t_host WHERE host_id = 'xxx'`
- **判断**：
  - region 有值 → 问题不在 region，进入步骤 2
  - region 为 NULL → 定位成功：纳管前未配置 region → 按文档补配后重试
  - 查不到该 host → 检查是否已纳管，进入步骤 3
-->

### 1. {{step_1_name}}

{{step_1_content}}

### 2. {{step_2_name}}

{{step_2_content}}

<!--
按需增减 ### 步骤。如果对话中只讨论了 2 步就加 2 步，
不要为了凑数编第 3 步。
-->

## 代码线索

{{code_clues}}

<!--
排查中涉及的关键代码位置。注意与 investigation 的区别——这里的代码线索不是"bug 在哪行"，
而是"代码证明了为什么缺了这一步会出这个结果"（如：参数校验的默认值逻辑、对缺失数据的处理分支、操作顺序依赖的代码路径）。

| 文件 | 行号 | 作用 |
|------|------|------|
| `path/to/file.py:100-150` | — | region 为空时跳过 host 的分支逻辑 |

如对话只提到模块名未到行号，注明功能描述即可，行号写 `（待定位）`。

同时列出关键日志关键字、配置项、环境变量等。
如对话未讨论，整段写"（代码线索未在对话中讨论，待补充）"。
-->

<!--
可选：## 常见误判
容易被混淆的相似问题及区分方法。
对话未讨论 → 写"（未在对话中讨论，待补充）"。
-->

<!--
可选：## 关联内容
指向相关 investigation、adr、arch、til。
格式：
- investigation: [[date-slug|标题]] — 具体排查案例
- adr: [[date-slug|标题]] — 相关决策背景
对话未讨论 → 留空或写"（待关联）"。
-->
